[build]
builder = "DOCKERFILE"
buildCommand = "docker build -t ecomate ."

[deploy]
startCommand = "docker run -p $PORT:80 ecomate"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10