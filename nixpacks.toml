[phases.setup]
nixPkgs = ['nginx', 'php82', 'php82Extensions.pdo', 'php82Extensions.pdo_mysql', 'php82Extensions.mysqli']

[phases.install]
cmds = ['echo "Installing dependencies..."']

[phases.build]
cmds = ['echo "Building application..."']

[start]
cmd = 'php -S 0.0.0.0:$PORT -t /app'